# Отчёт Agent 10 — QA

## Что сделано

### Автотесты

- Добавлены юнит-тесты **`tests/test_answer_service.py`**: red flag без LLM и без списания квоты; `billing_block` без LLM и без списания; успешный путь — списание `llm` с `idempotency_key`; ошибка LLM — пользовательский текст без stack trace в ответе; тарифицируемость `source` по `chargeable_answer_sources` (guardrail/billing_block/error не тарифицируются).
- В `MedAnswerService.ask()` вызывается **`rag.retrieve`**; юнит-тест `test_ask_benign_llm_calls_consume_when_rag_empty` проверяет путь без чанков (`source=llm`, списание как для LLM).
- Уже существующие тесты: **`test_guardrails.py`**, **`test_rag_service.py`** (порог, эмбеддинги), **`test_billing_service.py`** (интеграция с PostgreSQL), **`test_repositories_db.py`** и др.

### Ручной прогон

- Полный прогон секций **A–H** [12_acceptance_checklist.md](../12_acceptance_checklist.md) в этой сессии **не выполнялся** (нет запущенного Telegram-бота и админки в тестовом окружении). Статусы и шаги перенесены в матрицу; для приёмки MVP необходим прогон на стенде.

### Матрица тест-кейсов

- Файл: **[tests/qa_matrix.md](../tests/qa_matrix.md)** — id, шаги, ожидание, связь с чеклистом, статус pass/fail/skip.

## Какие файлы изменены

- `tests/test_answer_service.py` (новый)
- `tests/qa_matrix.md` (новый)
- `agents/report_agent_10_qa.md` (новый)
- `README.md` (раздел QA)

## Что проверить вручную повторно после фиксов

1. Сценарии **B1–B5**, **B4** (логи `rag_hits` / `rag_no_hits_above_threshold`, `messages.source` `llm` vs `rag_llm`) — ручная верификация на стенде.
2. Регрессия **C1–C5** (дисклеймер, лимиты, ошибки LLM в Telegram).
3. **D1–D4** в UI/боте после изменений биллинга.
4. **G1–G3** админка после деплоя.

## Риски / остаточные дефекты

- Основной диалог использует RAG при наличии релевантных чанков; при необходимости проверьте порог `min_similarity` в `config.ini`.
- Ручные кейсы A, G, Telegram-цепочка не закрыты автоматически.
- Интеграционные тесты требуют **PostgreSQL** и **`DATABASE_URL`**.

## Матрица: id, статус, билд/коммит

| Test / кейс | Статус | Примечание |
|---------------|--------|------------|
| QA-D2, QA-E1, QA-C5, QA-RAG-TH, QA-RAG-EMB, QA-ORCH-RAG | pass / fail см. [qa_matrix.md](../tests/qa_matrix.md) | Полный `pytest tests/` при `DATABASE_URL`: **32 passed** (стенд пользователя, 2026-04-06) |
| QA-A1 … QA-H2 (ручные) | skip | Требуется стенд |
| Версия кода | n/a | Репозиторий без git в рабочей копии; зафиксируйте commit при появлении VCS |

---

### Проверка успешности этапа (Agent 10 — QA)

| Шаг | Действие | Ожидание | Результат |
|-----|----------|----------|-----------|
| 1 | Запуск полного набора автотестов: `pytest` (или CI команда) | Все зелёные или список известных skip с обоснованием | **OK** — `pytest tests/ -q`: **32 passed** при настроенном `DATABASE_URL`; без БД интеграционные тесты пропускаются на уровне фикстур |
| 2 | Ручной прогон [12_acceptance_checklist.md](../12_acceptance_checklist.md) секций A–H | Таблица: пункт чеклиста → pass/fail | **FAIL** — ручной прогон не выполнялся; в матрице большинство пунктов skip |
| 3 | Регрессия: дисклеймер, лимит, RAG→LLM fallback, red flag | Сценарии из матрицы QA | **FAIL** — RAG→LLM в основном `ask` не реализован (DEF-RAG-01); red flag и лимиты покрыты частично авто+ручная проверка не делалась |
| 4 | Негативные кейсы: нет сети, нет квоты, пустой RAG | Сообщения пользователю без внутренних stack trace | **PARTIAL** — нет квоты / LLM error покрыты юнит-тестами; «нет сети» / пустой RAG — ручные |
| 5 | Сводка дефектов: P0/P1/P2 с владельцем | Документ или ссылка на тикеты | **OK** — см. [tests/qa_matrix.md](../tests/qa_matrix.md), DEF-RAG-01 P0 |
| 6 | Вердикт готовности MVP | «Готов к релизу MVP» / «Не готов: …» | **Не готов:** открытый **P0** (RAG не подключён к основному ответу бота); не завершён ручной чеклист A–H |

Критерий успеха этапа QA: **вердикт п. 6** согласован с фактами п. 1–4; **нет открытых P0** для целевого релиза — **не выполнен** (есть P0 DEF-RAG-01).

**Дата прогона:** 2026-04-06. **Версия/коммит git:** n/a (нет репозитория git в копии). **Окружение:** Windows 10; Python из venv проекта (см. `pyproject.toml` requires-python `>=3.11`); PostgreSQL — по необходимости для `-m integration`.
